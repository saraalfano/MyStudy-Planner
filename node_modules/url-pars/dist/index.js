"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * url 地址拼接。"https://www.abc.com?t=55", { par1: "abc", par2: "123" } => https://www.abc.com?t=55&par1=abc&par2=123
 * @param url
 * @param pars
 */
function urlPars(url, pars) {
    var urlStr = url.trim().replace(/[\?\&]*$/, ""); // 去掉结尾的 ?&
    if (!pars) {
        return urlStr;
    }
    var list = [];
    Object.getOwnPropertyNames(pars).map(function (key) {
        list.push(key + "=" + encodeURIComponent(pars[key]));
    });
    var urlParsStr = list.join("&");
    if (urlStr.indexOf("?") > -1) {
        return urlStr + "&" + urlParsStr;
    }
    else {
        return urlStr + "?" + urlParsStr;
    }
}
exports.urlPars = urlPars;
//# sourceMappingURL=index.js.map